'use strict';

module.exports = {
  migrate: require('./migrate'),
  serve: require('./serve'),
  seed: require('./seed'),
  secret: require('./secret'),
  scrape: require('./scrape'),
  rotate: require('./rotate')
};
